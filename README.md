  ALGORITHM:
  STEP->1:Initialize array,low=0,high=n-1,target;
  STEP->2:Use FOR LOOP
  STEP->3:Intialize Mid =low+(high-low)/2.
  STEP->4:If (arr[mid]==target).
  Then->   Print mid.
  STEP->5:Else if(arr[mid]<target)
   Then->    low=mid+1
  STEP->6:Else (high=mid-1).
  STEP->7:exit(return 0);
